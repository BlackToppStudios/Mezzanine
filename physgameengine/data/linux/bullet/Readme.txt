This is where libBullet.a belongs. If it does not exists make sure that the dependencies are install which include atleast freeglut3-dev, and maybe others.

Then use libincludes/linux/bulletsrc/BulletWorkspace.workspace with codeblocks to compile it.
